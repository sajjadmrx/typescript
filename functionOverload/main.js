function sum(x, y) {
    return x + "  " + y;
}
function totalLength(x, y) {
    var total = x.length + y.length;
    if (x instanceof Array) {
    }
    if (x instanceof String) {
    }
    return total;
}
